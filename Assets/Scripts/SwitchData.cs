using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SwitchData : MonoBehaviour
{
    public SavePlayerAttr attr;
    public PlayerAttributes player;
    // Start is called before the first frame update
    void Start()
    {
        //player = GameObject.Find("Players").GetComponent<PlayerAttributes>();
        //Debug.Log("passed");
        //if (player == null)
        //{
        //    Debug.Log("null");
        //}
        //player.maxHP = attr.maxHP;
        //player.maxMP = attr.maxMP;
        //player.atk = attr.atk;
        //player.def = attr.def;
        //player.crit = attr.crit;
        //player.critDmg = attr.critDmg;
        //player.doge = attr.doge;
        //player.currentHP = attr.currentHP;
        //player.currentMP = attr.currentMP;
        //attr.pass = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
